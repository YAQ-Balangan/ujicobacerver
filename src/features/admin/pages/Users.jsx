import React from "react";
import AdminDashboard from "../AdminDashboard";

export default function AdminUsers() {
  // Temporary wrapper: later extract user-management UI here.
  return <AdminDashboard initialTab={"siswa"} />;
}
